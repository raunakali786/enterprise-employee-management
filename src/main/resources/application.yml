spring:
  application:
    name: enterprise-employee-management

  datasource:
    url: jdbc:sqlserver://localhost:62055;databaseName=ems_db;encrypt=true;trustServerCertificate=true
    username: sa
    password: 786@Raunak
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
#    database-platform: org.hibernate.dialect.SQLServerDialect

server:
  port: 8080
